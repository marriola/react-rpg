export default class Coordinate {
    constructor(spriteWidth, spriteHeight, row, column) {
        this.spriteWidth = spriteWidth;
        this.spriteHeight = spriteHeight;
        this.row = row;
        this.column = column;
    }
}